# Copy to .env and fill in your own values: cp .env.example .env
# Read by backend/settings.py; unknown keys are ignored.

# Required. Get one at https://openrouter.ai/keys
OPENROUTER_API_KEY=sk-or-v1-...

# Optional. Skill directory the generator reads references from.
# SKILL_DIR=diagram-design

# Optional. Directory for stored sources, caches, and generated artifacts.
# JOBS_DIR=jobs
